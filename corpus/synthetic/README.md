# Synthetic fixtures

This directory is reserved for project-authored boundary and negative fixtures.
Each binary fixture must have a matching entry in `../manifest.jsonl`; being
synthetic does not exempt it from hash, size, provenance, and expected-result
tracking.

No binary fixture is included in the repository.
